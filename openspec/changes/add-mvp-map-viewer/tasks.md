## 1. 事前検証

- [ ] 1.1 JourneyMapローカルタイルディレクトリ構造(ズームレベル/X/Y命名規則)を実データで確認し、Web標準タイル形式(XYZ)との互換性を確認する(design.md Open Questions)
- [ ] 1.2 biomeデータ(8.2MB)の内容・用途を確認し、エクスポート時の扱いを決める(design.md Open Questions)

## 2. エクスポートスクリプト(map-data-export)

- [x] 2.1 Node.jsスクリプトのプロジェクト構成を作成する(実行はローカルPC、CI/CDには組み込まない)
- [x] 2.2 `.minecraft/journeymap/data`配下のOverworld day/night/topo/waypoints/biomeを走査する処理を実装する
- [x] 2.3 chunk_cacheディレクトリを走査・変換・出力の対象から除外する(allowlist方式)
- [ ] 2.4 タイル画像・waypoint JSON・biomeデータをWeb配信用構造に変換して出力する処理を実装する
- [ ] 2.5 ローカルデータ不在・フォーマット不整合時にエラーで停止する処理を実装する
- [ ] 2.6 実データでエクスポートスクリプトを実行し、出力構造を確認する

## 3. デプロイスクリプト(map-data-deploy)

- [ ] 3.1 Wrangler CLIおよびCloudflareアカウント認証情報のセットアップ手順を整備する
- [ ] 3.2 エクスポート出力ディレクトリをCloudflare R2バケットへフルシンク(全量上書き)アップロードするスクリプトを実装する
- [ ] 3.3 実データでデプロイスクリプトを実行し、R2上にファイルが反映されることを確認する

## 4. フロントエンド基盤(map-viewer)

- [ ] 4.1 react-leafletおよび関連依存をインストールする
- [ ] 4.2 地図ビュー画面(S-01)のルーティング・レイアウトを作成する
- [ ] 4.3 R2上のタイルURLをLeafletの`L.tileLayer`に接続し、初期レイヤー(昼)のタイルを表示する

## 5. レイヤー切替・座標表示(map-viewer)

- [ ] 5.1 昼/夜/地形のレイヤー切替UIを実装する
- [ ] 5.2 レイヤー切替時にTileLayerのURLテンプレートを切り替える処理を実装する
- [ ] 5.3 地図クリック時に座標を表示するUIを実装する
- [ ] 5.4 表示した座標をクリップボードにコピーする機能を実装する
- [ ] 5.5 対象タイルが存在しない場合に空白/デフォルト表示となることを確認する

## 6. デプロイ・結合確認

- [ ] 6.1 Cloudflare Pagesへのフロントエンドデプロイ設定を行う
- [ ] 6.2 推測困難なURLでの公開方針を確認する(認証機構は追加しない)
- [ ] 6.3 エクスポート→デプロイ→スマホからの地図表示という一連の操作を実施し、パン・ズーム・レイヤー切替・座標表示が動作することを確認する(要件定義書Q24のMVP成功基準)
